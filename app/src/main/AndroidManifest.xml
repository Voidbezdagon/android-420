<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cm.com.teamscheduler"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".app.app.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".app.app.login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".app.app.activity_user"
            android:label="@string/app_name" />
        <activity
            android:name=".app.app.activity_user_detailes"
            android:label="@string/app_name" />
        <activity
            android:name=".app.app.scheduleCalendar"
            android:label="@string/app_name" />
        <activity
            android:name=".app.app.activity_create_user"
            android:label="@string/app_name" />
        <activity
            android:name=".app.app.activity_edit_user"
            android:label="@string/app_name" />
        <activity
            android:name=".app.app.activity_position"
            android:label="@string/app_name" />
        <activity
            android:name=".app.app.activity_position_detailes"
            android:label="@string/app_name" />
        <activity
            android:name=".app.app.activity_create_position"
            android:label="@string/app_name" />
        <activity
            android:name=".app.app.scheduleDayList"
            android:label="@string/app_name" />
        <activity
            android:name=".app.app.scheduleDetails"
            android:label="@string/app_name" />
        <activity
            android:name=".app.app.scheduleCreateReport"
            android:label="@string/app_name" />
        <activity
            android:name=".app.app.activity_location"
            android:label="@string/app_name" />
        <activity
            android:name=".app.app.activity_location_details"
            android:label="@string/app_name" />
        <activity
            android:name=".app.app.activity_create_location"
            android:label="@string/app_name" />
        <activity
            android:name=".app.app.activity_location_items"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".app.app.activity_location_items_details"
            android:label="@string/app_name"></activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

    </application>

</manifest>